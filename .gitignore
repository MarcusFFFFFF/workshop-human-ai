# Sensitive — bot tokens, chat IDs, secrets
.claude/settings.local.json
.claude/settings.json

# Operativ state — sessions-specifik
.code_wake
.workshop_telegram_offset
.workshop_sent_outbound_ids

# Bridge-logg innehåller raw memory I/O — kan ha känsligt content
sessions/workshop_bridge.log

# STATE.md — överlämning mellan Code-instanser, läses lokalt från disk.
# Innehåller privata tredje parter och öppna sårbarhets-listor — får ej publiceras.
STATE.md

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Editor
.idea/
.vscode/
